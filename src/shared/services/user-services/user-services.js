const api = baseServices => ({
  async loadUser ({ id }, cancelToken) {
  },
  async saveUser ({ id, email, origpass, newpass, repeatpass, curatorGroups }, cancelToken) {
  },
  async deleteUser ({ id }, cancelToken) {
  },
})

export default api
